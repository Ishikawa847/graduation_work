<div class="group bg-[#242424] border border-white/5 rounded-[2rem] overflow-hidden hover:border-blue-500/30 transition-all duration-500 flex flex-col h-full shadow-2xl shadow-black/50">
  
  <%# 画像セクション %>
  <div class="relative w-full aspect-square overflow-hidden bg-[#1a1a1a]">
    <% if recipe.image.attached? %>
      <%= image_tag recipe.square_image, 
          alt: recipe.name, 
          class: "w-full h-full object-cover transition-transform duration-700 group-hover:scale-110",
          loading: "lazy" %>
    <% else %>
      <div class="w-full h-full flex flex-col items-center justify-center text-gray-700">
        <svg class="w-12 h-12 mb-2 opacity-20" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path d="M4 16l4.586-4.586a2 2 0 012.828 0L16 16m-2-2l1.586-1.586a2 2 0 012.828 0L20 14m-6-6h.01M6 20h12a2 2 0 002-2V6a2 2 0 00-2-2H6a2 2 0 00-2 2v12a2 2 0 002 2z"></path></svg>
        <span class="text-[10px] uppercase tracking-widest font-bold opacity-40">No Image</span>
      </div>
    <% end %>

    <%# 画像上のオーバーレイ（グラデーションと日付） %>
    <div class="absolute inset-0 bg-gradient-to-t from-[#242424] via-transparent to-transparent opacity-80"></div>
    <div class="absolute bottom-4 left-6">
      <span class="text-[10px] font-mono text-gray-400 bg-black/40 backdrop-blur-md px-2 py-1 rounded-md">
        <%= recipe.created_at.strftime("%Y.%m.%d") %>
      </span>
    </div>
  </div>
  
  <%# コンテンツセクション %>
  <div class="p-6 flex-grow flex flex-col justify-between space-y-4">
    <div>
      <h3 class="text-xl font-bold text-white tracking-tight mb-2 line-clamp-1 group-hover:text-blue-400 transition-colors">
        <%= link_to recipe.name, recipe_path(recipe) %>
      </h3>
      
      <p class="text-gray-400 text-sm font-light leading-relaxed line-clamp-2">
        <%= recipe.description.presence || "No description available." %>
      </p>
    </div>
    
    <%# PFC簡易インジケーター（追加！） %>
    <div class="grid grid-cols-3 gap-2 pt-2 border-t border-white/5">
      <div class="text-center">
        <p class="text-[8px] text-gray-500 uppercase font-bold mb-1">Prot</p>
        <div class="text-xs font-mono font-bold text-red-500"><%= recipe.total_protein.round(1) %><span class="text-[8px] ml-0.5 text-gray-600">g</span></div>
      </div>
      <div class="text-center border-x border-white/5">
        <p class="text-[8px] text-gray-500 uppercase font-bold mb-1">Fat</p>
        <div class="text-xs font-mono font-bold text-blue-500"><%= recipe.total_fat.round(1) %><span class="text-[8px] ml-0.5 text-gray-600">g</span></div>
      </div>
      <div class="text-center">
        <p class="text-[8px] text-gray-500 uppercase font-bold mb-1">Carb</p>
        <div class="text-xs font-mono font-bold text-yellow-500"><%= recipe.total_carb.round(1) %><span class="text-[8px] ml-0.5 text-gray-600">g</span></div>
      </div>
    </div>
  </div>
</div>
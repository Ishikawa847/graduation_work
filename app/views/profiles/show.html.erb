<div class="min-h-screen bg-gray-900 text-gray-100 py-8 px-4">
  <div class="max-w-7xl mx-auto">

    <!-- ページタイトル -->
    <h1 class="text-3xl font-bold mb-8 text-gray-100">      
      <%= t('.title') %>
    </h1>

    <!-- 左右2カラム -->
    <div class="grid grid-cols-1 lg:grid-cols-3 gap-8">

      <!-- 左側：レシピ一覧（2/3） -->
      <div class="lg:col-span-2">
        <div class="bg-gray-800 rounded-lg shadow-lg p-6">

          <div class="flex justify-between items-center mb-6">
            <h2 class="text-2xl font-bold text-gray-100">
              <%= t('.posted_recipes') %>
            </h2>
          </div>

          <% if @recipes.any? %>

            <div class="space-y-4">
              <% @recipes.each do |recipe| %>
                <div class="border border-gray-700 rounded-lg p-4 hover:shadow-xl transition-shadow duration-200 bg-gray-850">
                  <div class="flex gap-4">

                    <!-- レシピ画像 -->
                    <div class="flex-shrink-0">
                      <div class="w-32 h-32 bg-gray-700 rounded-lg overflow-hidden">
                        <% if recipe.image.present? %>
                          <%= image_tag recipe.square_image, alt: recipe.name, class: "w-full h-full object-cover" %>
                        <% else %>
                          <div class="w-full h-full flex items-center justify-center text-gray-400">
                            <span>No Image</span>
                          </div>
                        <% end %>
                      </div>
                    </div>

                    <!-- レシピ情報 -->
                    <div class="flex-grow">
                      <h3 class="text-xl font-bold text-gray-100 mb-2">
                        <%= link_to recipe.name, recipe_path(recipe), class: "hover:text-blue-400 transition-colors" %>
                      </h3>

                      <% if recipe.description.present? %>
                        <p class="text-gray-400 text-sm mb-3 line-clamp-2">
                          <%= truncate(recipe.description, length: 80) %>
                        </p>
                      <% end %>

                      <!-- 投稿日 -->
                      <div class="flex items-center text-sm text-gray-500 mb-3">
                        <svg class="w-4 h-4 mr-1" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4l3 3m6-3a9 9 0 11-18 0 9 9 0 0118 0z"></path>
                        </svg>
                        <%= l(recipe.created_at, format: :long) %>
                      </div>

                      <!-- ボタン -->
                      <div class="flex gap-2">
                        <%= link_to t('.edit'), edit_recipe_path(recipe),
                          class: "inline-flex items-center px-4 py-2 bg-blue-600 text-white text-sm font-medium rounded-lg hover:bg-blue-700 transition" %>

                        <%= link_to t('.delete'), recipe_path(recipe),
                          data: { turbo_method: :delete, turbo_confirm: "本当に削除しますか？" },
                          class: "inline-flex items-center px-4 py-2 bg-red-600 text-white text-sm font-medium rounded-lg hover:bg-red-700 transition" %>
                      </div>
                    </div>

                  </div>
                </div>
              <% end %>
            </div>

          <% else %>

            <div class="text-center py-12">
              <svg class="w-16 h-16 mx-auto text-gray-600 mb-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12h6m-6 4h6m2 5H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z"></path>
              </svg>
              <p class="text-gray-400 text-lg mb-4">
                <%= t('.no_recipes.message') %>
              </p>
              <%= link_to t('.no_recipes.create_first'), new_recipe_path,
                class: "inline-block px-6 py-3 bg-blue-600 text-white rounded-lg hover:bg-blue-700 transition" %>
            </div>

          <% end %>

        </div>
      </div>

      <!-- 右側：プロフィール（1/3） -->
      <div class="lg:col-span-1">
        <div class="bg-gray-800 rounded-lg shadow-lg p-6 sticky top-8">

          <div class="flex justify-between items-center mb-6">
            <h2 class="text-xl font-bold text-gray-100">
              <%= t('.profile_info.title') %>
            </h2>
            <%= link_to t('.profile_info.edit'), edit_profile_path, class: "text-blue-400 hover:text-blue-300 text-sm font-medium" %>
          </div>

          <!-- ユーザー名 -->
          <div class="mb-6 text-center">
            <div class="w-20 h-20 bg-gray-700 rounded-full mx-auto mb-3 flex items-center justify-center">
              <span class="text-3xl font-bold text-blue-400"><%= @user.name.first %></span>
            </div>
            <h3 class="text-xl font-bold text-gray-100"><%= @user.name %></h3>
            <p class="text-sm text-gray-400"><%= @user.email %></p>
          </div>

          <div class="space-y-4">
            <!-- 年齢 -->
            <div class="border-b border-gray-700 pb-4">
              <div class="flex justify-between">
                <span class="text-sm text-gray-400"><%= User.human_attribute_name(:age) %></span>
                <span class="text-lg font-bold text-gray-100">
                  <% if @user.age.present? %>
                    <%= t('.profile_info.age_unit', count: @user.age) %>
                  <% else %>
                    <%= t('.profile_info.unset') %>
                  <% end %>
                </span>
              </div>
            </div>

            <!-- 身長 -->
            <div class="border-b border-gray-700 pb-4">
              <div class="flex justify-between">
                <span class="text-sm text-gray-400"><%= User.human_attribute_name(:height) %></span>
                <span class="text-lg font-bold text-gray-100">
                  <% if @user.height.present? %>
                    <%= t('.profile_info.height_unit', count: @user.height) %>
                  <% else %>
                    <%= t('.profile_info.unset') %>
                  <% end %>
                </span>
              </div>
            </div>

            <!-- 体重 -->
            <div class="border-b border-gray-700 pb-4">
              <div class="flex justify-between">
                <span class="text-sm text-gray-400"><%= User.human_attribute_name(:weight) %></span>
                <span class="text-lg font-bold text-gray-100">
                  <% if @user.weight.present? %>
                    <%= t('.profile_info.weight_unit', count: @user.weight) %>
                  <% else %>
                    <%= t('.profile_info.unset') %>
                  <% end %>
                </span>
              </div>
            </div>

            <!-- 性別 -->
            <div class="pb-4">
              <div class="flex justify-between">
                <span class="text-sm text-gray-400"><%= User.human_attribute_name(:gender) %></span>
                <span class="text-lg font-bold text-gray-100"><%= @user.human_attribute_enum(:gender) %></span>
              </div>
            </div>

          </div>

        </div>
      </div>

    </div>
  </div>
</div>

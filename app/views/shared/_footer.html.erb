<footer class="bg-gray-900 text-gray-100 border-t border-gray-800 py-6 pt-12">
  <div class="container mx-auto px-6 flex flex-col md:flex-row items-center justify-between">

    <!-- 左側：コピーライト -->
    <p class="text-sm text-gray-400 mb-4 md:mb-0">
      <%= t('footer.copyright', year: Time.current.year) %>
    </p>

    <!-- 右側：各リンク -->
    <nav class="flex space-x-6 text-sm">
      <%= link_to t('footer.terms_of_service'), "#", class: "hover:text-gray-300 transition" %>
      <%= link_to t('footer.privacy_policy'), "#", class: "hover:text-gray-300 transition" %>
      <%= link_to t('footer.contact'), "#", class: "hover:text-gray-300 transition" %>
    </nav>

  </div>
</footer>
